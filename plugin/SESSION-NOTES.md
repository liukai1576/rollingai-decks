# Session Notes — Kangshifu Deck Tuning (2026-05-27)

> **Read this first** in any continuation session. Captures everything the
> prior turn-by-turn conversation would have given you. Pair with `CHANGELOG.md`
> (skill version history) and `README.md` (architecture overview).

---

## 🎯 Current goal

User is **case-by-case polishing** the rendered deck at
`imports/RollingAI分享-康师傅/render-output-full/index.html`.

They view it via `http://localhost:8765/index.html` (a `python3 -m http.server`
is running in the output directory).

---

## ⚖️ Hard workflow rules (do NOT violate)

1. **DO NOT run `build.py` / `run.sh`** unless user explicitly says "重抽" or
   "重 build". Every build overwrites `index.html` and wipes manual edits.
2. **Edit `index.html` directly** for content tweaks. Use grep + Edit tool.
3. **Skill code edits are fine** (build.py, iwa_resolver.py, extract.applescript
   etc.) — just don't execute. Future build picks them up.
4. **General/universal bugs → fix in code**. **One-off content tweaks → edit
   HTML directly.**
5. **DO NOT change the company logo aspect ratio**. Use `object-fit:contain`.

---

## 🗂️ Key files & paths

- `imports/RollingAI分享-康师傅/render-output-full/index.html` — the LIVE deck
- `imports/RollingAI分享-康师傅/render-output-full/deck.json` — intermediate (regenerated by build.py — don't trust manual edits there)
- `imports/RollingAI分享-康师傅/render-output-full/extract.tsv` — **mtime 05-27 02:31** (Keynote Creator Studio v15)
- `imports/RollingAI分享-康师傅/render-output-full/_renderer/` — local renderer assets (CSS/JS)
- `imports/RollingAI分享-康师傅/render-output-full/serve.sh` — `bash serve.sh` starts http server on :8765
- `imports/RollingAI分享-康师傅/redesigns/slide-NN.html` and `slide-NNN.html` — REDESIGN OVERRIDE files that survive rebuilds
- `.key` source: `/Users/liukai/Library/Mobile Documents/com~apple~Keynote/Documents/RollingAI分享 - 康师傅.key`

---

## ⏱️ Critical timestamp

- `extract.tsv` mtime: **2026-05-27 02:31** (last extract via Creator Studio v15)
- `.key` mtime: **2026-05-27 16:45** — user updated .key after extract
- **There are likely .key changes (e.g. page 57 text deletion) NOT in our TSV.**
  User has accepted manual HTML patches as substitute.

---

## 📋 PDF page → slide-key mapping (key ones)

| PDF | slide-key | notes |
|---|---|---|
| 3 | slide-003 | |
| 5 | slide-005 | master bg up@2x.png special handling |
| 6 | slide-006 | redesigned — case-study masonry |
| 7 | slide-012 | "Our Clients" logo wall |
| 9 | slide-015 | |
| 11 | slide-017 | bg = `assets/slide-017/curve-bg.png` (user-provided) |
| 12 | slide-018 | video on opaque-black shape |
| 13 | slide-019 | white-bg bug fix |
| 14 | slide-020 | left-align like 15 |
| 15 | slide-021 | reference for left-align |
| 16 | slide-022 | left-align |
| 17 | slide-023 | left-align |
| 18 | slide-024 | last 3 lines green |
| 19 | slide-025 | widened title |
| 22 | slide-036 | 经营管理模式 / 建立竞争壁垒 nowrap |
| 24 | slide-039 | redesigned (slide-24.html) — fonts bumped 30% |
| 31 | slide-050 | bg = `assets/slide-031/drone-bg.png` (user-provided in PDF-page-named dir) |
| 32 | slide-051 | |
| 35 | slide-054 | manual 5×5 table inserted |
| 37 | slide-056 | **AUDIO ALLOWED** |
| 41 | slide-061 | |
| 43 | slide-063 | |
| 45 | slide-066 | cyan highlight + left align |
| 46 | slide-067 | master up@2x hidden |
| 49 | slide-070 | **AUDIO ALLOWED** |
| 57 | slide-079 | text-stripped, only flow chart |
| 59 | slide-081 | nowrap on 经营管理模式 / 从全局一刀切 / 到局部最优解 |
| 61 | slide-083 | v2 redesign exists in `redesigns/slide-083.html` (user says messy) |

PDF page N → keynote_no varies (some keynote slides are "skipped" in deck).

---

## ✅ Manual HTML edits applied today (NOT in skill code)

### Page 3
- Removed 3 text divs: "我们构建面向未来的商业" / "Builders" / "Run Business in AI Native Way"

### Page 6 (slide-006)
- **Full redesign** replacing AppleScript-extracted layout with hand-authored
  CSS Grid masonry (`s6-wrap` / `s6-grid` / `s6-card` classes).
- 4 columns × 6 fixed-height rows (`grid-template-rows: repeat(6, 180px)`).
- 11 cards with character/mascot images as full-bleed backgrounds
  (`object-fit: contain` for dark scrim cards, `cover` for light/red/gold).
- Right panel: ROLLING AI logo + "AI商业咨询" + 3 stats (100+ 商业重塑, 1500+ 智能体, 10X+ 影响力).
- **User wants ALL cards UNIFORM** (same size + same fields + same layout) — task in progress when compaction triggered.
- **Card field spec for user**:
  - `bg`: full-bleed image (≥ 800×1000 portrait or 800×600 landscape)
  - `brand`: top-right logo (height ≤ 24px) OR text
  - `title`: top-left, 2 lines max, ≤ 6 chars/line
  - `metric`: bottom-left, e.g. "数字 单位"
- Logos pulled from `assets/slide-012/` (page 7 client wall):
  - AIA → `已粘贴的影片-25306.png`
  - ZIROOM 自如 → `image2-24575.png`
  - J&T 极兔速递 → `ebac768ac5f54def2ac91fa2a59582e0-25288.jpeg`
- Avatar image `generated_image_3-14488.png` has built-in white "S" frame — looks like inset sub-card in dark cards (inherent design, hard to fix in CSS alone)

### Page 7 (slide-012)
- FOTILE 方太 (file `478d0db1...jpeg`): `object-fit:contain` + `transform: scale(3)` centered
- 邮政储蓄银行 (file `已粘贴的影片-24567.png`): `object-fit:contain` + `transform: scale(1.5)` centered
- Schneider Electric (originally `image8-25327.png` = combined Schneider+Midea PNG):
  - Created cropped variant `image8-25327-schneider-only.png` (left 50% of original)
  - Updated HTML src to the cropped file

### Page 9 (slide-015)
- Body shape font 100px → 70px
- Wrapped first line in `<span color:#22FF09>` (kept green), last 2 lines `<span color:#FFFFFF>` (white)
- `display:flex` → `display:block` (so `<br>` works as line breaks, not as flex items)

### Page 11 (slide-017)
- Replaced `pasted-movie-15346.png` src → `assets/slide-017/curve-bg.png` (user-provided)
- Repositioned to (0,0) 1920×1080

### Page 12 (slide-018)
- Deleted the opaque-black iwa-fill div (`rgba(0,0,0,1.0)` covering 1931×1101)
  — it was hiding the video underneath

### Page 13 (slide-019)
- `background: #FFFFFF` → `background: #000` (heuristic misfire; this slide has video bg)

### Pages 14, 16, 17 (slide-020, slide-022, slide-023)
- Body text shape `text-align: center` → `text-align: left` (matching page 15)

### Page 18 (slide-024)
- Wrapped last 3 lines "颠覆生产方式" / "创造新产品新价值" / "开拓巨大的新需求和新市场" in `<span color:#22FF09>` (green)

### Page 19 (slide-025)
- "AI革命和电力革命本质是相同的" container: width 1184→1440, left 368→240, to fit on one line

### Page 22 (slide-036)
- "建立竞争壁垒" + "经营管理模式" shape divs: added `white-space:nowrap`

### Page 24 (slide-039) — already a `redesigns/slide-24.html` redesign
- Bumped font sizes ~30%:
  - `.r24-title`: 38→50
  - `.r24-head`: 18→26
  - `.r24-quote`: 13→18
  - `.r24-soln-label`: 11→14
  - `.r24-soln`: 15→22
  - `.r24-soln-body`: 12→17

### Page 31 (slide-050)
- Replaced master bg `image-2-1-9202.png` → `assets/slide-031/drone-bg.png` (user-provided)

### Page 35 (slide-054)
- Title widened (646→1100) + `white-space:nowrap`
- **Manually added a 5×5 HTML table** (Keynote `table` type was unsupported):
  - Header: 数据 (gray) | 信息 (gray) | 知识 (black) | 经验 (black) | 认知 (black)
  - 4 rows of body: cols 1-2 white text, cols 3-5 orange `#F26A4A`
  - Text content verbatim from user's reference image

### Page 43 (slide-063)
- Logo `image4-25283.png`: `object-fit:cover` → `object-fit:contain` (to preserve aspect)
- **User just flagged AGAIN at compaction: "RollingAI的logo被挤压了，公司logo永远不能改变比例"**
  — needs re-verification. Logo natural 372×75 (4.96 ratio), bbox 186×24 (7.75 ratio). With contain → renders 119×24 in container. Should NOT be squished. If user still sees squishing, look for ANOTHER logo image on this slide.

### Page 45 (slide-066)
- Title "企业的隐形经营智慧就是": wrap "隐形经营智慧" in cyan `#48B2FE`, all white otherwise
- Subtitle: wrap "蒸馏" in cyan, rest white
- Both left-aligned, `white-space:nowrap`, container widened to 1100w

### Page 46 (slide-067)
- Master `up@2x-9101.png` `display:none` (was creating dark band at top)

### Page 57 (slide-079)
- Deleted all old text divs (12 entries) AND old bg `image3-19620.jpeg`
- Kept only the new flow chart `AI时代能力价值变化_流程图-19756.png` + corner logo

### Page 59 (slide-081)
- "经营管理模式" added `white-space:nowrap`
- "从全局一刀切" / "到局部最优解" parent div: widened from 317px→385px, added `nowrap`, moved left:77→43

### Global (end of body)
- Injected `<script>` that re-mutes all videos NOT on slide-056 or slide-070
  (PDF 37 & 49). Listens to `play` and `volumechange` events in capture phase.

---

## 🔧 Skill code edits today (in `plugin/skills/...` — NOT executed)

### `keynote-to-html/assets/iwa_resolver.py`
- NEW `lookup_with_bbox()` → returns `(filename, bbox, has_mask)` tuple
- NEW `master_bbox()` → look up template image's true bbox (for `up@2x` etc.)
- `_strip_data_id_suffix` regex: `-\d+$` → `-\d{4,}$` (don't strip `-1` from `image-2-1`)
- `lookup()` now returns None when filename hint given but no stem matches (was incorrectly picking closest bbox)
- Walks `Index/Stylesheet.iwa` + `Index/Theme.iwa` for `TSWP.ShapeStyleArchive` to surface shape fills (incl. linear gradients) AppleScript misses
- Now indexes `TSWP.ParagraphStyleArchive` alignment via TSWP enum mapping `TATvalue0..4` → `left/right/center/justify/natural`
- Tracks `has_mask` from `TSD.ImageArchive.mask` field (not currently used — both phones and 邮储 have mask)

### `keynote-to-html/assets/build.py`
- `AssetResolver.resolve_with_bbox()` mirroring `resolve_for_element` but with IWA bbox + mask info
- Image render path: when AppleScript bbox OVERFLOWS canvas (off-canvas by >8px), use IWA's true bbox for masters (slide 5 up@2x case). Slide-level images: kept on AppleScript bbox (mask-aware override was too lossy).
- IWA shape-fill emit in-place via `_emit_iwa_fill_div()` — sources order respected (the fix for slide 18 video being hidden)
- NEW `_object_fit_for(nat_w, nat_h, render_w, render_h)`: returns `contain` when natural is wider than container (>25% drift, prevents 邮储/FOTILE-style side cropping); else `cover`
- Removed several "guess what Keynote would render" heuristics:
  - `has_slide_bg` master suppression (75% area threshold)
  - Container shape `rgba(255,255,255,0.12)` fake fill default
  - IWA-fill skip on opaque full-bleed shapes
- Auto-writes `serve.sh` + `_renderer/` folder (CSS/JS) for self-contained output

### `keynote-to-html/assets/extract.applescript`
- Targets `id "com.apple.Keynote"` (Creator Studio v15.2.1), not legacy `iWork.Keynote` (14.5)
- Skips extraction for slides marked `skipped: true` (still emits #SLIDE marker for numbering)

### `keynote-to-html/assets/run.sh`
- Uses `$KN_BUNDLE` (com.apple.Keynote) consistently
- `EXTRA_ARGS` empty-array fix: only passes if `${#EXTRA_ARGS[@]} > 0`

### `keynote-to-html/assets/build.py` — redesign override matching
- Accepts BOTH `slide-NN.html` (PDF index) AND `slide-NNN.html` (3-digit slide_key) in `--redesigns DIR`

### `rolling-deck-h5/deck-json/render-deck.py`
- Dropped the `if "/runs/" not in output_dir` gate on copy-assets (made `--inline` opt-out only)

---

## 🐛 Known issues NOT yet fixed

| Issue | Location | Status |
|---|---|---|
| Page 6 cards still varied sizes (small/medium/large) | slide-006 | **In progress** when compaction triggered. User wants UNIFORM cards. |
| Page 43 RollingAI logo aspect | slide-063 | **User flagged at compaction** — needs check |
| z-index discipline overrides Keynote draw order | global CSS in build.py | User said "改不改都可以" — deferred |
| Avatar `generated_image_3` has built-in S-frame creating sub-card illusion | image asset itself | Hard to fix without new avatar image |
| `extract.tsv` is stale vs `.key` (02:31 vs 16:45) | data | Continuing manual patches instead of re-extract |
| Page 61 v2 redesign "messy" per user | `redesigns/slide-083.html` | Awaiting direction |

---

## 🗨️ User stylistic preferences captured

- **Verbatim text rule**: All text MUST come from original Keynote (no editorial additions). Documented in `slide-redesign/SKILL.md` rule #1.
- **PPT-feel rule**: Redesigned slides fill 1920×1080 (≥ 80% width, ≥ 70% height). Documented as rule #5.
- **Company logo aspect**: NEVER change RollingAI logo aspect ratio.
- **Cards out of canvas**: OK if some content extends below 1080, but card proportions must hold.
- **Universal bugs go in skill code**. **One-off content tweaks go in result HTML**.
- **Don't trigger rebuilds**: a rebuild today already caused multiple regressions (yesterday's fixes lost). Always confirm before running.

---

## 🚀 Next actions when resumed

1. **Page 6 — UPDATED requirement (latest user direction)**:
   - **3 columns** (not 4), each scrolling upward as a vertical waterfall
   - All cards **uniform** size + uniform 4 fields (bg + brand + title + metric)
   - **Animated scroll**: each column translateY upward via CSS @keyframes
   - **错落有致**: different `animation-delay` per column so cards stagger visually
   - Loop: duplicate each column's cards so `translateY(0 → -50%)` loops seamlessly
   - Cards intentionally extend above/below canvas (cropped) for "scrolling river" feel
   - Drop the text-only 9.9K cards (or replace with placeholder so all 9 cards uniform)
   - Suggested distribution (3 cols × 3 cards = 9 known cases):
     - Col 1: 周大福 / MARS / AIA (avatar+cat+avatar)
     - Col 2: 蒙牛 / 飞鹤 / ZIROOM (robot+chicken+avatar)
     - Col 3: 极兔 / Amway / 双汇 (rabbit+avatar+lion)
   - Card spec: ~290w × 360h, uniform border-radius 22px

2. Verify page 43 RollingAI logo NOT squished — find which image element user sees as squished. Current `image4-25283.png` has `object-fit:contain` which should preserve aspect. Maybe user is looking at a different logo.

3. Continue case-by-case per user's instructions.

---

## 🔄 Compaction handoff

User has triggered compaction. After their `/compact`:

1. **READ THIS FILE FIRST** before anything else.
2. **Don't ask user to recap** — everything you need is in this file + `CHANGELOG.md` + `README.md`.
3. **Resume from "Next actions" above**, not from "what should I do next".
4. The slide-006 HTML in `index.html` is mid-edit (still 4-col, non-uniform). User has requested 3-col scrolling animation — needs full rewrite of `<style>` and `<div class="s6-grid">` content.
